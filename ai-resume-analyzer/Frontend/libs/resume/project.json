{
  "name": "resume",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "root": "Frontend/libs/resume",
  "sourceRoot": "Frontend/libs/resume/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project resume --web",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/resume",
        "main": "src/index.ts",
        "tsConfig": "tsconfig.lib.json",
        "assets": ["*.md"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "Frontend/libs/resume/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  }
}
